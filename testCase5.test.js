import DecryptingEmojiCode from './testCase5';

describe('test case 5 ', () => {
  test('test case 1 Input: racecar Output: true', () => {
    expect(DecryptingEmojiCode('racecar')).toBe('racecar');
  });
});
